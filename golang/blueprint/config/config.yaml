appName: "MyApp"
version: "1.0"
serverConfig:
  - type: http
    port: 8080
  - type: graphql
    port: 8081
dbConfig:
  type: postgres
  postgres:
    host: localhost
    port: 5432
    username: postgres
    password: mypassword
    dbname: postgres
