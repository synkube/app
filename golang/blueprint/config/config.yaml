appName: "MyApp"
version: "1.0"
serverConfig:
  - type: http
    port: 8080
  - type: grpc
    port: 9090
dbConfig:
  type: postgres
  postgres:
    host: localhost
    port: 5432
    username: postgres
    password: mypassword
    dbname: postgres
